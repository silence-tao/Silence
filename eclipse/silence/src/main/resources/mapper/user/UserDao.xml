<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.silencetao.dao.user.UserDao">
	<insert id="insertUser">
		INSERT INTO USER(username, nikename, PASSWORD, register_ip, user_sign)
		VALUES(#{username}, #{nikename}, #{password}, #{registerIp}, #{userSign})
	</insert>
	
	<delete id="deleteUser">
		DELETE FROM USER WHERE user_id = #{id}
	</delete>
	
	<update id="updateUser">
		UPDATE USER SET nikename = #{nikename}, PASSWORD = #{password}, 
		user_sign = #{userSign} WHERE user_id = #{userId}
	</update>
	
	<select id="getUser" resultType="User">
		SELECT * FROM USER WHERE (username = #{username} OR nikename = #{nikename}) 
		AND PASSWORD = #{password}
	</select>
	
	<select id="getCountByNikename" resultType="java.lang.Integer">
		SELECT COUNT(1) FROM USER WHERE nikename = #{nikename}
	</select>
	
	<select id="getCountByUsername" resultType="java.lang.Integer">
		SELECT COUNT(1) FROM USER WHERE username = #{username}
	</select>
</mapper>